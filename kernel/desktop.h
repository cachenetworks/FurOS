#ifndef FUR_DESKTOP_H
#define FUR_DESKTOP_H

/* Main desktop entry point — shows the KDE Plasma-style text desktop,
 * launches the shell when the user opens the Terminal, and loops forever. */
__attribute__((noreturn)) void desktop_run(void);

#endif
